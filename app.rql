@settings {
  useSnowflakes boolean // defines if snowflakes (global ids) are used, default false, only possible if using multiple models.
  @ Integer (inc) // not required if #useSnowflakes is false.
}

model This {
  @ Integer (inc) // not required if #useSnowflakes is true
  name String
}

model That {
  name String
}